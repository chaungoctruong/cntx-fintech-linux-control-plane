# Optional semantic retrieval stack — NOT used by default Docker build (see requirements.txt header).
# Do not install on small nodes unless AI_PLATFORM_KNOWLEDGE_VECTOR_ENABLED=true is intentionally enabled.
sentence-transformers>=3.0,<4
