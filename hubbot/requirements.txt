# Hubbot runtime (see hubbot/Dockerfile). Python 3.11+ recommended to match backend image.
# Backend API uses separate pins in backend_ai/backend/requirements.txt — do not unify blindly.
python-telegram-bot[webhooks]==21.6
python-dotenv==1.0.1
httpx==0.27.2
psutil>=5.0.0
aio-pika>=9.0.0
